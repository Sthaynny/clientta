{
  "name": "clientta-functions",
  "description": "Cloud Functions — Stripe Billing para Clientta",
  "engines": {
    "node": "22"
  },
  "main": "index.js",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "test": "node --test test/*.test.js",
    "reset-user": "node scripts/reset_user_by_email.js"
  },
  "dependencies": {
    "firebase-admin": "^13.10.0",
    "firebase-functions": "^7.2.5",
    "stripe": "^22.3.1"
  },
  "private": true
}
